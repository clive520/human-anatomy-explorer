# 人體解剖互動網站 — 核心功能上線！

我們已經成功完成了人體解剖網站最關鍵的「**3D 探索器核心 (骨骼系統)**」！
網站已經部署到 Firebase，您可以直接在線上遊玩與測試。

## 🌐 線上預覽

> [!TIP]
> **馬上體驗：** [https://human-anatomy-explorer-18e1b.web.app](https://human-anatomy-explorer-18e1b.web.app)

*(模型檔案有 26MB，初次載入可能需要等候 5~10 秒，畫面會有 Loading 提示)*

---

## ✨ 實作亮點

### 1. 3D 骨骼互動模型
我們找到並整合了一個 26MB 的高畫質 `skeleton.glb` 模型。
- **自動適應**：系統會自動計算模型的 Bounding Box，將它置中並縮放到最適合的比例，確保一打開就能看到全貌。
- **自由旋轉**：支援滑鼠左鍵拖曳旋轉、右鍵平移、滾輪縮放。

### 2. 左側：系統控制台
- **系統圖層切換**：目前「骨骼系統」可勾選開啟或隱藏（其他系統標示為建置中）。
- **透明度滑桿**：即時調整 3D 模型的透明度。
- **快速視角**：點擊「正面 / 背面 / 左側 / 右側」按鈕，相機會自動飛到對應位置。

### 3. 右側：資訊面板與點擊偵測
- **部位點擊 (Raycasting)**：在 3D 畫布上點擊骨骼的任何位置，會自動套用發光的高亮材質！
- **智慧資訊卡**：點擊後，右側會滑出資訊面板。透過我們在 `skeleton.js` 中建立的資料庫，系統會抓取點擊部位的名稱（中英對照）與解剖學說明。

---

## 📸 實機畫面

這是我們在本地測試時的自動化截圖：

![3D 探索器介面](file:///C:/Users/hp/.gemini/antigravity/brain/a3abac6c-3d69-41ad-8241-75e106c193d3/.system_generated/click_feedback/click_feedback_1778280657807.png)

*註：自動化代理人在擷取圖片時，剛好位於載入瞬間，因此模型較暗。請直接前往線上版體驗完整的 3D 光影效果！*

---

## 🛡️ 安全部署流程

本次的修改同樣遵循了我們在「鹿陽專案」建立的嚴格規範：
1. 本地開發與測試 `npm run deploy:safe`。
2. 自動將包含 26MB 模型的程式碼推送到您的 GitHub `clive520/human-anatomy-explorer`。
3. 自動發布到 Firebase Hosting。

## 接下來的計畫
目前「骨骼系統」的 MVP（最小可行性產品）已經非常完整。若要繼續擴充：
1. 我們可以開始尋找或處理「**肌肉、血管、神經**」的 3D 模型。
2. 擴充 `js/data/skeleton.js` 裡面的部位中文說明，把全部 200 多塊骨頭的細節補齊。
